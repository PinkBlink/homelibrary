import java.io.File;

public class UtilsForFiles {
    private static final String SEPARATOR = File.separator;
    private static final String PATH_TO_CATALOG = "." + SEPARATOR + "catalog" + SEPARATOR;
    private static final String PATH_TO_USERS = "." + SEPARATOR + "users" + SEPARATOR;
}
